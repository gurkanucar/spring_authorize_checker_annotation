package com.gucardev.spring_boot_authorize_checker.model;

/** The enum Role. */
public enum Role {
  /** Admin role. */
  ADMIN,
  /** User role. */
  USER,
  /** Restaurant admin role. */
  RESTAURANT_ADMIN
}
